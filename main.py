# How Tic Tac Toe will work
# 2 players enter their names and designation
# double array in main class keeps count of 
# 

"""
from PyQt5.QtWidgets import *
from PyQt5.QtCore import *
from PyQt5.QtGui import *

class MainWindow(QMainWindow):
    
    def __init__(self):
        super().__init__()

        #board setup
        board = []
        for i in range(3):
            board.append([0,0,0])

        self.setAutoFillBackground(True)
        p = self.palette()
        p.setColor(self.backgroundRole(), Qt.white)
        self.setPalette(p)
        self.setFixedSize(1000,500)

        
"""


#how the game works
# get the name first
# then setup the board
# print beginning instructions


